# VGG16训练过程中的要点

1.  与之前的*LeNet*和*AlexNet*不同，VGG16因为模型较深，所以参数的初始值会对模型训练结果产生较大影响。若随机初始值不恰当，则将出现梯度爆炸问题，模型无法收敛。 
解决办法是，对比*LeNet*和*AlexNet*，在model.py文件中加入参数初始化方法，具体来说则是kaiming初始化和正态分布初始化。

2. 在实际训练中发现，批次batch越大，模型越容易收敛，反之，若批次过小，则也容易梯度爆炸。此时，模型是否收敛将会变成每次训练时的一个概率问题。经测试，能基本保证模型收敛的最小批次是24。